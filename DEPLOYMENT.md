# 🚀 دليل النشر على Vercel

## خطوات النشر

### 1️⃣ رفع المشروع على GitHub

```bash
# تهيئة Git
git init

# إضافة جميع الملفات
git add .

# عمل Commit
git commit -m "Initial commit: ToolX Full Stack Application"

# إنشاء Repository على GitHub ثم ربطه
git remote add origin https://github.com/YOUR_USERNAME/toolx.git

# رفع الكود
git branch -M main
git push -u origin main
```

### 2️⃣ النشر على Vercel

#### الطريقة الأولى: من خلال موقع Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. سجل دخول باستخدام حساب GitHub
3. اضغط على **"New Project"**
4. اختر الـ Repository: `toolx`
5. اضغط على **"Deploy"**

#### الطريقة الثانية: من خلال CLI

```bash
# تثبيت Vercel CLI
npm install -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel

# النشر للإنتاج
vercel --prod
```

### 3️⃣ إعدادات البيئة على Vercel

في لوحة تحكم Vercel، اذهب إلى:
**Settings → Environment Variables**

أضف المتغيرات التالية:

```
NODE_ENV=production
MAX_FILE_SIZE=52428800
UPLOAD_DIR=/tmp
```

### 4️⃣ ملاحظات مهمة

⚠️ **تنبيهات Vercel:**

1. **حجم الملفات**: Vercel لديه حد أقصى 50MB لحجم الطلب
2. **Serverless Functions**: لديها حد زمني 10 ثواني (Hobby) أو 60 ثانية (Pro)
3. **التخزين المؤقت**: استخدم `/tmp` للملفات المؤقتة
4. **FFmpeg**: قد تحتاج لاستخدام layer خاص أو خدمة خارجية

### 5️⃣ البدائل للأدوات الثقيلة

بعض الأدوات مثل معالجة الفيديو قد لا تعمل بشكل مثالي على Vercel Serverless.

**الحلول:**

1. **استخدام خدمة منفصلة للـ Backend:**
   - Railway.app
   - Render.com
   - Heroku
   - DigitalOcean

2. **استخدام خدمات خارجية:**
   - Cloudinary (للصور والفيديو)
   - AWS Lambda (مع layers)
   - Google Cloud Functions

### 6️⃣ هيكل النشر الموصى به

```
Frontend (Vercel)
    ↓
API Gateway (Vercel Serverless)
    ↓
Heavy Processing (Railway/Render)
    ↓
Storage (AWS S3 / Cloudinary)
```

## 🔧 تحديثات مستقبلية

```bash
# بعد أي تعديل
git add .
git commit -m "وصف التعديل"
git push

# Vercel سيقوم بالنشر تلقائياً
```

## 📊 مراقبة الأداء

- **Vercel Dashboard**: لمراقبة الطلبات والأخطاء
- **Analytics**: تفعيل Vercel Analytics
- **Logs**: مراجعة السجلات في الوقت الفعلي

## 🌐 الدومين المخصص

1. اذهب إلى **Settings → Domains**
2. أضف الدومين الخاص بك
3. اتبع تعليمات DNS

---

**ملاحظة**: هذا المشروع يعمل بشكل أفضل على خادم تقليدي (VPS) بسبب معالجة الملفات الكبيرة.
للاستخدام الكامل، يُنصح باستخدام Railway أو Render للـ Backend.
