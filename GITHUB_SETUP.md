# 📋 خطوات رفع المشروع على GitHub

## ✅ تم الإعداد بنجاح!

تم تهيئة Git وإضافة جميع الملفات. الآن اتبع الخطوات التالية:

---

## 🔥 الخطوة 1: إنشاء Repository على GitHub

1. اذهب إلى [GitHub](https://github.com)
2. اضغط على **"New Repository"** أو **"+"** → **"New repository"**
3. املأ البيانات:
   - **Repository name**: `toolx` (أو أي اسم تريده)
   - **Description**: `ToolX - Full Stack Online Tools Platform`
   - **Public** أو **Private** (اختر ما تريد)
   - ❌ **لا تختر** "Add a README file" (لأننا لدينا واحد بالفعل)
4. اضغط **"Create repository"**

---

## 🔥 الخطوة 2: ربط المشروع المحلي بـ GitHub

بعد إنشاء الـ Repository، ستظهر لك صفحة بها أوامر. استخدم هذه الأوامر:

### إذا كان Repository جديد تماماً:

```bash
git remote add origin https://github.com/YOUR_USERNAME/toolx.git
git branch -M main
git push -u origin main
```

**استبدل `YOUR_USERNAME` باسم المستخدم الخاص بك على GitHub!**

---

## 🔥 الخطوة 3: رفع الكود

```bash
git push -u origin main
```

إذا طُلب منك تسجيل الدخول:
- أدخل اسم المستخدم وكلمة المرور
- أو استخدم **Personal Access Token** (موصى به)

### كيفية إنشاء Personal Access Token:

1. اذهب إلى: **GitHub** → **Settings** → **Developer settings** → **Personal access tokens** → **Tokens (classic)**
2. اضغط **"Generate new token"** → **"Generate new token (classic)"**
3. اختر الصلاحيات: `repo` (كل الصلاحيات)
4. اضغط **"Generate token"**
5. **انسخ الـ Token** (لن تراه مرة أخرى!)
6. استخدمه بدلاً من كلمة المرور عند الـ push

---

## 🚀 الخطوة 4: النشر على Vercel

### الطريقة الأولى: من خلال موقع Vercel (الأسهل)

1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط **"Sign Up"** أو **"Log In"**
3. اختر **"Continue with GitHub"**
4. بعد تسجيل الدخول، اضغط **"Add New..."** → **"Project"**
5. اختر الـ Repository: `toolx`
6. اضغط **"Import"**
7. **لا تغير أي إعدادات**، فقط اضغط **"Deploy"**
8. انتظر 1-2 دقيقة... ✨
9. **تم! موقعك أصبح على الإنترنت!** 🎉

### الطريقة الثانية: من خلال Vercel CLI

```bash
# تثبيت Vercel CLI
npm install -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel

# للنشر في الإنتاج
vercel --prod
```

---

## 🎯 بعد النشر

سيعطيك Vercel رابط مثل:
```
https://toolx-xxxxx.vercel.app
```

يمكنك:
- ✅ مشاركة الرابط مع أي شخص
- ✅ ربط دومين مخصص
- ✅ مراقبة الأداء من لوحة تحكم Vercel

---

## ⚠️ ملاحظات مهمة

### 1. متغيرات البيئة على Vercel

بعد النشر، اذهب إلى:
**Project Settings** → **Environment Variables**

أضف:
```
NODE_ENV=production
MAX_FILE_SIZE=52428800
UPLOAD_DIR=/tmp
```

### 2. قيود Vercel Serverless

- ⏱️ **Timeout**: 10 ثواني (Hobby) / 60 ثانية (Pro)
- 📦 **حجم الطلب**: 4.5MB (Hobby) / 50MB (Pro)
- 💾 **التخزين**: `/tmp` فقط (مؤقت)

### 3. الأدوات التي قد لا تعمل بشكل كامل

- 🎬 **معالجة الفيديو** (FFmpeg ثقيل)
- 📄 **ملفات PDF الكبيرة جداً**

**الحل**: استخدم خدمة منفصلة للـ Backend مثل:
- Railway.app
- Render.com
- Heroku

---

## 🔄 التحديثات المستقبلية

عند أي تعديل على الكود:

```bash
git add .
git commit -m "وصف التعديل"
git push
```

**Vercel سيقوم بالنشر تلقائياً!** 🚀

---

## 📞 المساعدة

إذا واجهت أي مشكلة:
- راجع [Vercel Documentation](https://vercel.com/docs)
- راجع [GitHub Documentation](https://docs.github.com)

---

## ✨ تم!

الآن لديك:
- ✅ مشروع على GitHub
- ✅ موقع على الإنترنت (Vercel)
- ✅ نشر تلقائي عند كل تحديث

**استمتع بموقعك! 🎉**
